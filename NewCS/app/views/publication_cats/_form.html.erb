<%= form_for @p_cats, :remote => true do |f| %>
  <% if @p_cats.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@p_cats.errors.count, "error") %> prohibited this publication_cat from being saved:</h2>

      <ul>
        <% @p_cats.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "Добавить категорию", :class=>"button_send_message button_blue"%>
  </div>
<% end %>
